# Repository Restructure

This repository was restructured on 2026-04-12.

All previous project files and directories were moved into the `legacy/` folder to preserve historical content while preparing the repository root for a fresh start.

## What changed

- Created `legacy/` and moved all existing files/directories there (except `.git`).
- Added a new root `README.md` (this file).
- Added a new root `CHANGELOG.md` documenting the restructure.

## Legacy content

You can find the prior repository contents under:

- `legacy/`
